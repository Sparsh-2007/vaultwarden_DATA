services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden_local
    restart: unless-stopped
    expose:
      - "80"
    environment:
      - SIGNUPS_ALLOWED=true       # Toggle to false AFTER you register your account
      - INVITATIONS_ALLOWED=true
      - WEBSOCKET_ENABLED=true
    volumes:
      - ./vaultwarden_data:/data   # Maps data directly into your local directory
    networks:
      - proxy-network

  caddy:
    image: caddy:2-alpine
    container_name: local_https_proxy
    ports:
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./localhost.pem:/certs/localhost.pem:ro
      - ./localhost-key.pem:/certs/localhost-key.pem:ro
    networks:
      - proxy-network
    restart: unless-stopped

networks:
  proxy-network:
    driver: bridge